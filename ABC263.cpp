#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> list(5);
    for (int i = 0; i < 5; i++)
        cin >> list.at(i);
    sort(list.begin(), list.end())
    if (list.at(0) == list.at(1) && list.at(1) == list.at(2)))
}
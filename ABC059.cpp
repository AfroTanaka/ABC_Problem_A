#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s, t, u;
    cin >> s >> t >> u;
    cout << (char)(s.at(0) - 32) << (char)(t.at(0) - 32) << (char)(u.at(0) - 32) << endl;
}